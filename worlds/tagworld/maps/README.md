# TagWorld maps

Map families and variant layouts for benchmark gates.

v1.1 corridor map is embedded in `tagworld_init_map()` / `tagworld_reset()`.

v1.1.1 will add tool-pressure maps where `PUSH_BLOCK_TO_DOORWAY` is required.
